const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function main() { const hashedPassword = await bcrypt.hash('12345678', 12); await prisma.user.create({ data: { username: 'melgendi11', email: 'melgendi11@vista.com', fullName: 'Mahmoud Elgendi', passwordHash: hashedPassword, isVerified: true } }); console.log('User created'); } main().finally(() => prisma.$disconnect());
